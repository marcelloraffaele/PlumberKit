# HTTP port the server listens on (default: 3000)
PORT=3000

# Set to "true" to skip GitHub signature verification during local development.
# NEVER set this to "true" in production.
SKIP_SIGNATURE_CHECK=false
